<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:color="@color/text_light_dark" android:state_selected="false"/> <!-- un selected -->
    <item android:color="@color/common_blue" android:state_selected="true"/> <!-- selected -->
    <item android:color="@color/text_light_dark"/> <!-- default 这个要在最底下，不然没有效果-->
</selector>
